const passingScore = 60;
const studentScore = 71;

//studentScore > passingScore ? console.log('Exam passed') : console.log('Exam failed');

if (studentScore > passingScore) {
   console.log('Exam passed');
} else {
    console.log('Exam failed');
}